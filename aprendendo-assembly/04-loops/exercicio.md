# Exercício 04 — Loop

## Tarefa

1. Entenda o que `dec`, `cmp` e `jne` fazem.
2. Mude o valor inicial de `rcx`.
3. Explique com suas palavras quando o loop para.

## Dica

- `dec rcx` diminui `rcx` em 1.
- `cmp rcx, 0` compara `rcx` com 0.
- `jne` significa: pule se não for igual.
